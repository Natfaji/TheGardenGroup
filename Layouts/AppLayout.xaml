<UserControl x:Class="UI.Layouts.AppLayout"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UI.Layouts"
			 xmlns:ViewModel="clr-namespace:UI.ViewModel"
			 mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

	<Grid x:Name="gMainWindow">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="200"/>
			<ColumnDefinition Width="15"/>
			<ColumnDefinition Width="*"/>
			<ColumnDefinition Width="15"/>
		</Grid.ColumnDefinitions>

		<Grid.RowDefinitions>
			<RowDefinition Height="50"/>
			<RowDefinition Height="15"/>
			<RowDefinition/>
			<RowDefinition Height="15"/>
		</Grid.RowDefinitions>

		<Label x:Name="loggedInAsLabel" Content="{Binding LoggedInAs}" FontSize="16" Grid.Column="2" VerticalAlignment="Center"/>

		<Grid Grid.Row="2">
			<Grid.RowDefinitions>
				<RowDefinition Height="auto"/>
				<RowDefinition Height="auto"/>
				<RowDefinition Height="auto"/>
				<RowDefinition Height="*"/>
				<RowDefinition Height="auto"/>
			</Grid.RowDefinitions>

			<Button Grid.Row="0" Content="Dashboard" Command="{Binding NavigateDashboardCommand}" FontSize="18" Padding="20,5,20,5" Height="50" Background="{x:Null}" BorderBrush="{x:Null}" HorizontalContentAlignment="Left"/>
			<Button Grid.Row="1" Content="Tickets" Command="{Binding NavigateTicketsCommand}" FontSize="18" Padding="20,5,20,5" Height="50" Background="{x:Null}" BorderBrush="{x:Null}" HorizontalContentAlignment="Left"/>
			<Button Grid.Row="2" Content="Employees" Command="{Binding NavigateEmployeesCommand}" FontSize="18" Padding="20,5,20,5" Height="50" Background="{x:Null}" BorderBrush="{x:Null}" HorizontalContentAlignment="Left"/>

			<Button Grid.Row="4" Content="Log out" Command="{Binding LogoutCommand}" FontSize="18" Padding="20,5,20,5" Height="50" Background="{x:Null}" BorderBrush="{x:Null}" HorizontalContentAlignment="Left"/>
		</Grid>

		<ContentControl Content="{Binding CurrentViewModel}" Grid.Column="2" Grid.Row="2"/>
	</Grid>
</UserControl>
