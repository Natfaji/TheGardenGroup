<UserControl x:Class="UI.Views.Dashboard"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:local="clr-namespace:UI.Views"
			 xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
			 mc:Ignorable="d"
			 d:DesignHeight="450" d:DesignWidth="800" >

	<Grid Background="White">

		<Grid.ColumnDefinitions>
			<ColumnDefinition/>
			<ColumnDefinition/>
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition/>
		</Grid.RowDefinitions>
		<TextBlock Text="Tickets overview" FontSize="30" TextAlignment="center" Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2"/>
		
		<Grid Grid.Row="1" Grid.Column="0">
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto"/>
				<RowDefinition/>
			</Grid.RowDefinitions>

			<lvc:CartesianChart Name="chart"
								Grid.Row="1"
								HorizontalAlignment="Stretch"
								VerticalAlignment="Stretch"
								Series="{Binding Series}"
								>
				<lvc:CartesianChart.Title>
					<lvc:XamlDrawnLabelVisual
						Text="Tickets for this year"
						Paint="#303030"
						TextSize="25"
						Padding="15"/>
				</lvc:CartesianChart.Title>

				<lvc:CartesianChart.XAxes>
					<lvc:AxesCollection>
						<lvc:XamlAxis AxisName="Week"/>
					</lvc:AxesCollection>
				</lvc:CartesianChart.XAxes>

				<lvc:CartesianChart.YAxes>
					<lvc:AxesCollection>
						<lvc:XamlAxis AxisName="Tickets"/>
					</lvc:AxesCollection>
				</lvc:CartesianChart.YAxes>
			</lvc:CartesianChart>
		</Grid>
	</Grid>
</UserControl>