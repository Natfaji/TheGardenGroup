<Window x:Class="UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI"
		xmlns:views="clr-namespace:UI.Views"
        xmlns:viewmodels="clr-namespace:UI.ViewModel"
        xmlns:layouts="clr-namespace:UI.Layouts" 
        mc:Ignorable="d"
        Title="MainWindow"
		Height="600" MinHeight="700" Width="1200" MinWidth="600"
		HorizontalAlignment="Center" VerticalAlignment="Center"
		HorizontalContentAlignment="Center"
		WindowStartupLocation="CenterScreen">

	<ContentControl Content="{Binding CurrentViewModel}">
		<ContentControl.Resources>
			<DataTemplate DataType="{x:Type viewmodels:AppLayoutViewModel}">
				<layouts:AppLayout />
			</DataTemplate>

			<DataTemplate DataType="{x:Type viewmodels:CreateEditTicketViewModel}">
				<views:CreateEditTicket />
			</DataTemplate>

			<DataTemplate DataType="{x:Type viewmodels:CreateEmployeeViewModel}">
				<views:CreateEmployee />
			</DataTemplate>

			<DataTemplate DataType="{x:Type viewmodels:DashboardViewModel}">
				<views:Dashboard />
			</DataTemplate>

			<DataTemplate DataType="{x:Type viewmodels:LoginLayoutViewModel}">
				<layouts:LoginLayout />
			</DataTemplate>

			<DataTemplate DataType="{x:Type viewmodels:LoginViewModel}">
				<views:LoginView />
			</DataTemplate>

			<DataTemplate DataType="{x:Type viewmodels:MainViewModel}">
				<local:MainWindow />
			</DataTemplate>

			<DataTemplate DataType="{x:Type viewmodels:TicketDetailsViewModel}">
				<views:TicketDetails />
			</DataTemplate>

			<DataTemplate DataType="{x:Type viewmodels:TicketsOverviewViewModel}">
				<views:TicketOverview />
			</DataTemplate>

			<DataTemplate DataType="{x:Type viewmodels:UserManagementViewModel}">
				<views:UserManagement />
			</DataTemplate>
		</ContentControl.Resources>
	</ContentControl>
</Window>