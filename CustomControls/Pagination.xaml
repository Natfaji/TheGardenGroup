<UserControl x:Class="UI.CustomControls.Pagination"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UI.CustomControls"
             mc:Ignorable="d">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*"/>
			<ColumnDefinition Width="*"/>
			<ColumnDefinition Width="*"/>
		</Grid.ColumnDefinitions>

		<Label Grid.Column="0" Width="135" HorizontalAlignment="Left" VerticalAlignment="Center"/>

		<StackPanel Orientation="Horizontal" Grid.Column="1" Width="135" VerticalAlignment="Center">
			<Label Content="Items per page" HorizontalAlignment="Center" VerticalAlignment="Center" />
			<ComboBox
				HorizontalAlignment="Center"
				VerticalAlignment="Center"
				SelectedValue="{Binding ItemsPerPage, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"
				SelectedValuePath="Content"
				SelectionChanged="ComboBox_SelectionChanged">
				<ComboBoxItem Content="10"/>
				<ComboBoxItem Content="25" />
				<ComboBoxItem Content="50" />
				<ComboBoxItem Content="100" />
			</ComboBox>
		</StackPanel>

		<StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="10" Grid.Column="2">
			<Button Command="{Binding PreviousPageCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" Content="Previous" Margin="5"/>
			<TextBlock Text="{Binding PageNumber, RelativeSource={RelativeSource AncestorType=UserControl}}" FontSize="16" VerticalAlignment="Center" Margin="5"/>
			<Button Command="{Binding NextPageCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" Content="Next" Margin="5"/>
		</StackPanel>
	</Grid>
</UserControl>
